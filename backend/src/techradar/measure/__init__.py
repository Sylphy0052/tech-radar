"""パラメータ実測のための計測モジュール（Issue #74、Issue #73 の前提）。

実装時の暫定値のまま根拠が残っていない 3 項目（LLM 投入サイズ上限 / クラスタ数の
決定方法 / フィード構成比の縮退動作）を測るための集計を提供する。値の変更そのものは
行わない（Issue #73 の担当範囲）。

本番 DB を参照するため、セッションは読み取り専用で開く（`session.read_only_session`）。
"""
